{
  "name": "wxstream-remote-sim",
  "version": "1.0.0",
  "description": "Weather stream audio simulator — chunks audio on silence and uploads to S3/MongoDB",
  "main": "dist/main.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start": "node dist/main.js",
    "dev": "ts-node src/main.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "dotenv": "^16.4.5",
    "mongodb": "^6.8.0",
    "wavefile": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "prettier": "^3.8.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  }
}
